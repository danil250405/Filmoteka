<!DOCTYPE html>
<html lang="en"
      xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Registration</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
          rel="stylesheet"
          integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
          crossorigin="anonymous">
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/home">Registration and Login System</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="./register.mustache">Register</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
<h2 class="text-center">Registration</h2>

<form role="form" action="/register/save" method="post" name="user" >


    <div class="form-group mb-3">
        <label class="form-label">Username</label>
        <input class="form-control"
               id="username"
               name="username"
               placeholder="Enter username"
               type="text"/>

        <div th:if="${#fields.hasErrors('username')}" class="alert alert-danger" id="error-message">
            <p>AHAHAHAHAH</p>
            <span th:text="${username}"></span>
        </div>

    </div>


    <div class="form-group mb-3">
        <label class="form-label">First Name</label>
        <input class="form-control"
               id="firstName"
               name="firstName"
               placeholder="Enter first name"
               type="text"/>
        {{#firstNameErrors}}
            <p class="text-danger">{{firstNameErrors}}</p>
        {{/firstNameErrors}}
    </div>

    <div class="form-group mb-3">
        <label class="form-label">Last Name</label>
        <input class="form-control"
               id="lastName"
               name="lastName"
               placeholder="Enter last name"
               type="text"/>
        {{#lastNameErrors}}
            <p class="text-danger">{{lastNameErrors}}</p>
        {{/lastNameErrors}}
    </div>

    <div class="form-group mb-3">
        <label class="form-label">Email</label>
        <input class="form-control"
               id="email"
               name="email"
               placeholder="Enter email"
               type="text"/>
        {{#email}}
            <p class="text-danger">{{emailErrors}}</p>
        {{/email}}
    </div>

    <div class="form-group mb-3">
        <label class="form-label">Password</label>
        <input class="form-control"
               id="password"
               name="password"
               placeholder="Enter password"
               type="text"/>
        {{#password}}
            <p class="text-danger">{{password}}</p>
        {{/password}}
    </div>


    <div class="form-group">
        <button type="submit">Register</button>
        <span>Already registered? <a href="/login">Login here</a></span>
    </div>
    {{#success}}
        <div class="alert alert-info">
            You have successfully registered our app!
        </div>
    {{/success}}
    <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
</form>
</body>
</html>